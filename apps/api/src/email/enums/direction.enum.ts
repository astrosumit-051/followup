/**
 * Conversation Direction
 *
 * Represents the direction of communication in conversation history:
 * - SENT: Message sent by the user to the contact
 * - RECEIVED: Message received from the contact
 */
export enum Direction {
  SENT = 'SENT',
  RECEIVED = 'RECEIVED',
}
